#include "geometry.hpp"
#include "utils.hpp"
#include <iostream>

using std::array;

template <std::size_t N>
int make_voronoi(array<point, N> points)
{
    return 0;
}

int main()
{
    std::printf("%d", avg(1, 4, 5));
}